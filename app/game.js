
var app = new Vue({
    el: '#app',
    data: {
      message: 'Hello Vue!'
    }
})


// Generate Equation
// Pick Operator
// if + pick two numbers less then 50
// if - pick 2 numbers less then 100 with the first greater then the second
// if x pick 2 numbers less then 20
// if div pick 2 numbers multiply them, then use the solution as partA


// Check Answer
// check if its the last question
// add +1 to problems solved else update error count
// call generate equation


// Check for key press
// R - resets app
// Enter - calls check answer


// Show Win Screen
// Display final time and errors